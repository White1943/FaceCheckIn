## 一、课题进展情况

本周我将主要精力投入到签到任务的创建与管理模块的设计与实现。签到任务是本系统教师端的重要功能，直接关系到学生考勤、课程管理等核心业务。通过本周的开发，我完成了签到任务的数据模型设计、任务创建接口、参数校验、权限控制等关键环节，并对任务的有效期、签到范围、异常处理等细节进行了深入实践和优化。
## 二、主要代码实现与说明

### 1. 数据模型设计（文件：app/models.py）
为支持灵活的签到任务管理，我在app/models.py中新增了AttendanceTask模型，并与课程、教师建立了关联。
```python
class AttendanceTask(db.Model):
    __tablename__ = 'attendance_tasks'
    task_id = db.Column(db.Integer, primary_key=True, autoincrement=True)
    course_id = db.Column(db.Integer, db.ForeignKey('courses.course_id'), nullable=False)
    teacher_id = db.Column(db.Integer, db.ForeignKey('users.user_id'), nullable=False)
    start_time = db.Column(db.DateTime, nullable=False)
    end_time = db.Column(db.DateTime, nullable=False)
    location_lat = db.Column(db.Numeric(10, 7))
    location_lng = db.Column(db.Numeric(10, 7))
    status = db.Column(db.Enum('active', 'ended', 'cancelled'), nullable=False)
    created_at = db.Column(db.DateTime, default=datetime.now)
    # 关系
    course = db.relationship('Course', backref='attendance_tasks')
    teacher = db.relationship('User', backref='created_tasks')
```
我的模型设计中，每个签到任务关联一个课程和一个教师，任务包含标题、描述、起止时间、签到地点等信息，便于后续扩展，通过created_at字段记录任务创建时间，便于排序和查询。
### 2. 签到任务创建接口实现（文件：app/attendance/views.py）

#### 教师创建签到任务

教师可通过接口为指定课程创建签到任务，后端需校验教师身份、课程归属、参数有效性等。
```python
@teacher_attendance_bp.route('/tasks', methods=['POST'])
@jwt_required()
def create_task():
    try:
        user_id = int(get_jwt_identity())
        data = request.get_json()
        required_fields = ['courseId', 'startTime', 'endTime']
        for field in required_fields:
            if field not in data:
                return Result.error(f"缺少必填字段: {field}")
        course = Course.query.get_or_404(data['courseId'])
        if course.teacher_id != user_id:
            return Result.error("无权在此课程发起签到", code=403)
        current_date = datetime.now().date()
        start_time = datetime.combine(
            current_date,
            datetime.strptime(data['startTime'], '%H:%M').time()
        )
        end_time = datetime.combine(
            current_date,
            datetime.strptime(data['endTime'], '%H:%M').time()
        )
        if end_time < start_time:
            end_time = datetime.combine(
                current_date + timedelta(days=1),
                datetime.strptime(data['endTime'], '%H:%M').time()
            )
        task = AttendanceTask(
            course_id=data['courseId'],
            teacher_id=user_id,
            start_time=start_time,
            end_time=end_time,
            status='active'
        )
        db.session.add(task)
        db.session.commit()
        return Result.success(message="签到任务创建成功")
    except Exception as e:
        print(f"Create attendance task error: {str(e)}")
        return Result.error("创建签到任务失败")
```

#### 查询课程下的签到任务

教师可查询课程下的所有签到任务， 。
```python
@teacher_attendance_bp.route('/tasks', methods=['GET'])
@jwt_required()
def get_tasks():
    try:
        user_id = int(get_jwt_identity())
        current_time = datetime.now()       
        # 首先更新所有已过期但未结束的任务
        expired_tasks = AttendanceTask.query.filter(
            AttendanceTask.end_time <= current_time,
            AttendanceTask.status == 'active'
        ).all()       
        # 记录自动结束的任务数量
        auto_ended_count = len(expired_tasks)       
        for task in expired_tasks:
            task.status = 'ended'          
        if expired_tasks:
            db.session.commit()
            print(f"自动结束了 {auto_ended_count} 个过期任务")     
        course_id = request.args.get('courseId')
        task_type = request.args.get('type', 'active')  # active 或 history
        query = AttendanceTask.query.filter_by(teacher_id=user_id)
        if course_id:
            query = query.filter_by(course_id=course_id)
        if task_type == 'active':
            query = query.filter_by(status='active')
        else:
            query = query.filter(AttendanceTask.status != 'active')
        tasks = query.order_by(AttendanceTask.created_at.desc()).all()
        # 获取每个任务的签到统计
        result_items = []
        for task in tasks:
            total_students = task.course.students.count()
            # 获取已签到学生数
            checked_in = AttendanceRecord.query.filter_by(
                task_id=task.task_id
            ).filter(
                AttendanceRecord.status != '缺课'
            ).count()
            result_items.append({
                'taskId': task.task_id,
                'courseId': task.course_id,
                'courseName': task.course.course_name,
                'startTime': task.start_time.strftime('%H:%M'),
                'endTime': task.end_time.strftime('%H:%M'),
                'date': task.start_time.strftime('%Y-%m-%d'),
                'status': task.status,
                'attendanceRate': f'{checked_in}/{total_students}',
                'createdAt': task.created_at.strftime('%Y-%m-%d %H:%M:%S')
            })
        return Result.success(data={
            'items': result_items,
            'autoEndedCount': auto_ended_count
        })

    except Exception as e:
        print(f"Get attendance tasks error: {str(e)}")
        return Result.error("获取签到任务列表失败")

```
学生查看能够未结束的，自己所选课程发布的签到
```python
@student_attendance_bp.route('/active', methods=['GET'])
@jwt_required()
def get_active_attendance_tasks():
    try:
        user_id = int(get_jwt_identity())
        now = datetime.now()
        # 获取学生已选课程的当前有效签到任务
        tasks = AttendanceTask.query.join(Course).join(CourseStudents).filter(
            CourseStudents.student_id == user_id,
            AttendanceTask.status == 'active',
            AttendanceTask.end_time > now
        ).all()
        # 排除已签到的任务
        signed_task_ids = set(
            record.task_id for record in AttendanceRecord.query.filter_by(
                student_id=user_id
            ).all()
        )
        return Result.success(data={
            'items': [{
                'taskId': task.task_id,
                'courseId': task.course_id,
                'courseName': task.course.course_name,
                'teacherName': task.course.teacher.real_name,
                'startTime': task.start_time.strftime('%H:%M'),
                'endTime': task.end_time.strftime('%H:%M')
            } for task in tasks if task.task_id not in signed_task_ids]
        })
    except Exception as e:
        print(f"Get active attendance tasks error: {str(e)}")
        return Result.error("获取签到任务失败")

```
### 3. 参数校验与权限控制

- 所有接口均通过@jwt_required()保证只有登录用户可操作。

- 创建任务接口严格校验用户身份和课程归属，防止越权操作。

- 必填参数等均有详细校验，避免无效或异常数据入库。
### 4. 任务有效期与业务逻辑

- 任务的start_time和end_time决定了签到的有效期，后续学生签到时需校验是否在有效期内。

- 通过location字段支持地理位置签到，为后续扩展打下基础。
### 5. 与前端协作

- 明确接口参数和返回格式，前端可直接通过接口获取课程下的所有签到任务，实现任务展示与管理。

- 创建任务接口返回详细的错误提示，便于前端处理异常场景。

- 时间格式采用统一的字符串格式，便于前后端数据交互。
## 三、遇到的问题与解决方案

- 时间格式解析异常

在接口开发初期，前端传递的时间格式与后端解析格式不一致，导致解析失败。通过与前端协商，统一采用'%Y-%m-%d %H:%M:%S'格式，并在后端增加异常捕获和详细错误提示。

- 权限校验遗漏

初期接口未对课程归属进行严格校验，导致教师可为非本人课程创建签到任务。后续在接口中增加course.teacher_id == user_id判断，确保权限安全。

- 参数缺失与校验

部分前端请求未传递所有必填参数，导致后端报错。通过missing列表收集缺失参数，并在接口返回详细提示，提升接口健壮性和用户体验。

## 四、下一阶段计划

- 实现学生签到接口，支持学生在有效期内完成签到，并记录签到状态。

- 支持签到任务的地理位置校验、人脸识别等高级功能。

- 优化签到任务的查询与展示，支持分页、筛选等功能。

- 编写相关单元测试，提升代码健壮性和可维护性。
## 五、存在问题或困难

- 目前签到任务仅支持单一地点和时间段，后续如需支持多地点、多时段需进一步设计。

- 任务删除、修改等接口尚未实现，需补充完善。

- 任务与学生签到记录的关联查询在大数据量下的性能需持续关注和优化。
## 六、结语

本周通过对签到任务创建与管理模块的开发，我深入理解了后端业务建模、接口设计、参数校验和权限控制等关键技术点。通过实际编码和问题解决，提升了对Flask下复杂业务流程的掌控能力。后续将继续完善签到相关功能，丰富系统的考勤管理能力，为实现高效、智能的教学管理系统打下坚实基础。